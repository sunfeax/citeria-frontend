export interface iServiceList {
  id: string;
  specialistId: string;
  specialistName: string;
  name: string;
  description: string;
  priceAmount: number;
  durationMinutes: number;
  currency: string;
  isActive: boolean;
  createdAt: string;
}
